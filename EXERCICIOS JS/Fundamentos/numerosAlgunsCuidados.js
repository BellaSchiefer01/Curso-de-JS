console.log(7/0) //Geralmente o resultado seria que não é divisível por 0, Porém no JS ele monstra como Infinity
console.log("10" / 2) //Por mais que o 10 esteja como String nesse caso por ter somente número ele cosegue dividir
console.log("Show" * 2) //Não permite executar aparece como NaN no console
console.log("10" * 2) //Nesse caso como já visto permite pois só a String só tem número.
console.log(0.1 + 0.7)
/*Os computadores representam números em binário, e alguns números decimais,
como 0.1, não podem ser representados exatamente em binário. Assim, quando
somamos esses números, o resultado pode ser um valor muito próximo,
mas não exatamente o que esperamos.
 */

console.log((10.345) .toFixed(2))

console.log('3' + 2)//Aqui ele contatena.